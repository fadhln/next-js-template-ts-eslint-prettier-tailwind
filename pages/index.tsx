import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <div
      className={
        'flex bg-slate-100 w-full h-[100vh] items-center justify-center'
      }
    >
      Hello Next.js!
    </div>
  );
};

export default Home;
